[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
gunicorn = "*"
opencv-python = { version = ">=4.0,<5.0" }
pillow = { version = ">=6.0,<7.0" }
responder = "*"

[requires]
python_version = "3.7"
